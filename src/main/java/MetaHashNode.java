/*
MetaHashNode is an extension of MyHashElement that is used to store meta-data obtained from parsed websites. It is used
to distinguish meta-data from regular website text.
 */

public class MetaHashNode extends MyHashElement {
    public MetaHashNode(int v, String k) { super(v,k); }
}
